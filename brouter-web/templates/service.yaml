apiVersion: v1
kind: Service
metadata:
  name: {{ include "brouter-web.fullname" . }}
  labels:
    {{- include "brouter-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "brouter-web.selectorLabels" . | nindent 4 }}
