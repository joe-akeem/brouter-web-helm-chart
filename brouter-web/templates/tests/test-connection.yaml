apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brouter-web.fullname" . }}-test-connection"
  labels:
    {{- include "brouter-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brouter-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
